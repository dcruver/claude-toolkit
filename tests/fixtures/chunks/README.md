# chunks

A bundle whose concepts exist for `okf chunk`: one Tier 1 class carrying
several methods, one record carrying a `# Schema`, and one concept whose body
is mostly fenced code blocks full of lines that look like SPEC.md §4 headings
and are not.

The sources are small and real, but nothing here reads them — chunking is a
body-only operation.
