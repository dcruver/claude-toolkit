# PLAN.md — OKF Code Knowledge

Checklist for `ralph --plan PLAN.md`. Every item is one fresh-context attempt with no memory
of any other. Shared design decisions live in `SPEC.md` (read-only — never check anything off
there). Items are ordered so none depends on a later one. Every item verifies with
`./tests/toolkit.sh`, which item 1 creates.

## Phase 1 — Tier A: `bin/okf`

- [x] Add tests/toolkit.sh, an executable dependency-free bash harness providing assert_eq, assert_contains, assert_exit, and a with_fixture_repo helper that copies a directory from tests/fixtures/ into a temp dir, runs git init in it and executes a callback there, plus repo-invariant checks that every commands/*.md has YAML frontmatter with a non-empty description and that bash -n passes on install.sh and bin/ralph, printing a per-check pass/fail summary and exiting non-zero if any check fails (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Add bin/okf as an executable bash script with subcommand dispatch and a --help covering every subcommand in SPEC.md §7, a preflight that exits 1 naming exactly which of bash, git, sha256sum, awk, sed, rg, jq and curl are missing, the SPEC.md §7 exit-code contract, global -C and --config flags, and a working okf init that writes okf.json with the SPEC.md §6 defaults plus a bundle-root index.md of type Codebase carrying the only legal okf_version key and refusing to overwrite either without --force, with tests added to tests/toolkit.sh (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf list subcommand in bin/okf to print in-scope source files honouring .gitignore via git ls-files plus the bundle include, exclude and extensions settings from okf.json, where --missing restricts output to sources having no co-located concept file and --orphans instead lists concept files whose resource no longer exists, with tests over a tests/fixtures/ tree covering all three modes (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf hash and okf fanin subcommands in bin/okf, where hash prints the sha256: prefixed digest of one file and fanin prints a whole-word ripgrep reference count for a type name across in-scope files with the declaration site excluded as SPEC.md §5 describes, with tests asserting a known digest, a known reference count, and that a name nothing references returns zero (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Add frontmatter helper functions to bin/okf that read and rewrite OKF concept files under the SPEC.md §4 formatting contract, exposing a reader for the fields the shell needs (type, resource, status, stale_after, generated.at, generated.by, verified entry timestamps, and code.content_hash, code.tier, code.symbol, code.language) and a writer that sets one field in place while preserving every other line and unknown key byte for byte, with tests proving an unknown top-level key and an unknown key inside the code: block both survive a rewrite (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf check subcommand in bin/okf to report drifted concepts whose stored code.content_hash differs from the current sha256 of their resource, in-scope sources with no concept, and orphan concepts, per SPEC.md §8, always exiting 0 unless --strict which exits 3 when drift exists, supporting --json output, and offering --stamp to write stale_after at the detection instant without ever removing an existing verified entry, with tests covering each of those states (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf index subcommand in bin/okf to regenerate a per-directory index.md of type Package listing that directory's concepts and subdirectories as bundle-absolute markdown links for OKF progressive disclosure, leaving the bundle-root index.md's okf_version intact and never writing an okf_version key into any non-root index.md, with tests over a nested fixture tree (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf verify subcommand in bin/okf to append a verified entry with the --by actor defaulting to human: plus the current username and the current UTC timestamp in ISO 8601, preserving all unknown frontmatter keys, and to print the resulting SPEC.md §8 trust tier including its degradation to Machine-confirmed when the new entry predates generated.at or the concept is drifted, with tests for each outcome (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh

## Phase 2 — install and slash commands

- [ ] Extend install.sh to copy bin/okf to $HOME/.local/bin/okf alongside ralph and report it in the same style, warning without failing about any missing SPEC.md §3 runtime prerequisite, add the read-only okf list, okf hash, okf fanin, okf check and okf search entries to permissions.json, and add .okf/ to .gitignore (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Add commands/okf-init.md and commands/okf-generate.md following the house style of commands/onboard.md with YAML frontmatter, an explicit stopping point and explicit non-goals, where okf-init runs okf init and reports scope via okf list --missing without authoring any prose, and okf-generate reads each in-scope source, extracts its declared types, calls okf fanin for the ranking signal, assigns a tier by the SPEC.md §5 rules, and writes co-located concept files obeying the SPEC.md §4 formatting contract stamped generated.by claude-code/<model> with no verified entry (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Add commands/okf-refresh.md and commands/okf-verify.md following the house style of commands/onboard.md, where okf-refresh runs okf check, re-authors the bodies of drifted concepts and updates their code.content_hash and generated.at without removing any verified entry, and okf-verify walks draft or drifted concepts with the user and stamps each confirmed one via okf verify --by human:<id> (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh

## Phase 3 — integration with /onboard and ralph

- [ ] Extend commands/onboard.md with an OKF step placed after the CLAUDE.md write and after the verified baseline build, running okf init and then generating concepts for in-scope files, skipping cleanly with a printed note when the okf script is not installed, and preserving onboard's existing stopping point of not moving into planning or code changes (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Extend bin/ralph so each normal non-[mvn] attempt's PROMPT includes the co-located concept docs for the source files its item names, via a shell helper that extracts path-like tokens from the item text, resolves each to its sibling .md concept file, and emits nothing at all when okf is not installed or no sibling concept exists, leaving [mvn] item handling and the review gate strings untouched (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Extend bin/ralph so a normal non-[mvn] item's PROMPT instructs the attempt to refresh the co-located concept doc of every source file it modified and include those refreshed files in the same commit, stamping generated.by as ralph/<model> and never writing a verified entry as SPEC.md §11 requires, skipping that step silently when okf is absent and leaving [mvn] item handling untouched (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh

## Phase 4 — Tier B: semantic index

- [ ] Implement the okf chunk subcommand in bin/okf to split one concept into a JSON array of summary, method and schema chunks on the body headings defined in SPEC.md §4, emitting exactly one summary chunk built from title, description and code.signature when a Tier 0 concept has no body, and carrying the full SPEC.md §9 payload field set on every chunk including the computed trust tier, with tests for a Tier 0 stub, a Tier 1 concept with several methods, and a record with a Schema section (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf embed subcommand in bin/okf to request embeddings from the OpenAI-shaped /v1/embeddings endpoint configured in okf.json using curl, create the Qdrant collection over its REST API with cosine distance and embedding_dim when it does not exist, and upsert one point per chunk with the SPEC.md §9 payload and a deterministic UUIDv5-shaped point id, exiting 2 with the missing-key hint when the index config block is absent, with tests driving a fake curl placed on PATH and asserting the request bodies (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Implement the okf search subcommand in bin/okf to embed the query text, search Qdrant over REST with optional repo and type payload filters and a --k limit, group hits by concept_id so a method hit returns its parent concept alongside it, show each result's trust tier, and under --hyde-prompt print the HyDE prompt and exit without ever calling an LLM as SPEC.md §9 requires, with tests driving a fake curl placed on PATH (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Add commands/okf-search.md following the house style of commands/onboard.md, which calls okf search --hyde-prompt, answers the printed HyDE prompt itself, passes the resulting hypothetical concept body back as the query text, presents results grouped by concept with their trust tier shown, and falls back to a ripgrep sweep over co-located concept files with a printed note when okf.json has no index block (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
- [ ] Update README.md to document the okf component: what OKF is, the Tier A and Tier B split with Tier B opt-in, that MCP is deliberately deferred, the five new slash commands, the okf subcommands from SPEC.md §7, the okf.json keys, the SPEC.md §3 runtime prerequisites, and okf entries added to the Install and Uninstall sections alongside ralph (see SPEC.md's Design Reference). Verify: ./tests/toolkit.sh
